# Execution Wave — Definition of Done

This wave is complete when:

- external verification/credential messages are sent and tracked;
- original infopacks for Thrive and Step are parsed;
- material conflicts remain explicit rather than guessed;
- private CRM, dossiers and Todoist reflect the same state;
- Triglav is removed from active execution after deadline with receipt ambiguity preserved;
- active GitHub override records current facts;
- exact-head CI passes before merge.
