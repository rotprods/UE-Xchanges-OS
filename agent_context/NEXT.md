# /next — UE-Xchanges-OS

Status: **STABLE ROUTER**  
Context: `CTX-UEX-GLOBAL-EXPANSION-INCOME-V1`

The canonical deep continuity directive remains:

[`NEXT_DEATHSAFE.md`](NEXT_DEATHSAFE.md)

The mandatory application/outreach execution policy is:

[`../docs/APPLICATION_EXECUTION_CONTRACT.md`](../docs/APPLICATION_EXECUTION_CONTRACT.md)

Do not execute an older frontier from this file, a historical checkpoint or chat memory.

## Mandatory behavior

1. Read current GitHub `main`.
2. Follow `AGENTS.md` + `agent_context/bootstrap_manifest.json` completely.
3. Read `docs/SEMANTIC_BRAIN_DURABILITY.md`.
4. Read `docs/APPLICATION_EXECUTION_CONTRACT.md`.
5. Read [`NEXT_DEATHSAFE.md`](NEXT_DEATHSAFE.md).
6. Reconstruct the latest private Drive/EventBus/lease/RuntimeGraph/provider truth.
7. Produce the `RECOVERY READBACK` required by `NEXT_DEATHSAFE.md`.
8. Select the highest-value safe **current** frontier under the execution-first ordering.
9. Enter `/AUTONOMOUS-GAUNTLET-LOOP` only after that reconciliation.

## Execution-first selection law

When a viable application, provider-requested action, receipt reconciliation or deadline-sensitive opportunity can be advanced safely with existing machinery, that work outranks non-blocking architecture, prompt design, semantic infrastructure, documentation or refactor work.

Architecture may pre-empt execution only for a demonstrated blocker/safety defect, a mandatory versioned gate, or an explicit user request for architecture.

Do not create a new control plane merely because a previous one is imperfect. Find the smallest failing enforcement point and repair it.

## Non-negotiable

```text
chat memory != continuity
service liveness != persistence
restore before regenerate
semantic similarity != mutation authority
projection existence != projection freshness
historical frontier != current frontier
architecture activity != application outcome
email sent != form submitted != selected
unknown send outcome != permission to resend
```

A cold agent must never treat this router as live domain state. Newer authoritative evidence always wins.
