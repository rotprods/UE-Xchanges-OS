# UE-Xchanges-OS — NEXT DEATH-SAFE MASTER METAPROMPT

Status: **CANONICAL COLD-START CONTINUATION DIRECTIVE**  
Context: `CTX-UEX-GLOBAL-EXPANSION-INCOME-V1`  
Authority: derived navigation only. Fresh provider/receipt + private Drive/EventBus + current GitHub always wins.

# /AUTONOMOUS-GAUNTLET-LOOP
## COLD-START · DEATH-SAFE · EXECUTION-FIRST · GLOBAL-BARRIER-AWARE

Retoma `rotprods/UE-Xchanges-OS` as a completely new agent. Never execute a remembered or historical frontier.

## 1. Recovery loop

```text
READ CURRENT MAIN
→ OBEY bootstrap_manifest.json
→ READ REQUIRED PRIVATE AUTHORITY
→ RECONCILE GLOBAL BARRIERS
→ RECONCILE PROVIDER/GMAIL/FORM EVIDENCE FOR THE CANDIDATE FRONTIER
→ SELECT HIGHEST-VALUE SAFE CURRENT ACTION
→ REGISTER FRESH SESSION
→ BOOTSTRAP ACK
→ WRITER AUTHORIZATION
→ EXACT LEASE
→ EXECUTE ONE BOUNDED ACTION
→ VERIFY / RECEIPT
→ PERSIST
→ RELEASE
→ HANDOFF
```

## 2. North Star

`APPLY EVERYTHING VIABLE`, optimising for truthful receipt-backed applications, real selections/acceptances, attended/certified experiences and verified paid outcomes — never architecture volume.

## 3. Current frontier selection

Always compute the frontier from current evidence. Order:

1. provider-requested action / live selection / acceptance;
2. deadline or place at risk;
3. complete candidature through an authorised route;
4. receipt/outcome reconciliation;
5. source verification required to execute;
6. blocking control-plane/projection repair;
7. non-blocking engineering/architecture.

A historical checkpoint, scheduler milestone, PR list or old Human_Now row is not a current frontier.

## 4. INFOPACK-FIRST

Before organiser questions about project facts:

`ORIGINAL INFOPACK → CURRENT CALL → CURRENT FORM → OFFICIAL ORGANISER/PARTNER SOURCE → EXISTING THREAD → UNRESOLVED MATERIAL BLOCKER ONLY`.

Do not send `REDUNDANT_SOURCE_RESOLVABLE` questions.

## 5. INBOUND_ACTION_FIRST

Classify organiser/provider inbound as:

`ACTION_REQUEST | OUTCOME | INFORMATION | TERMINAL`.

If it is an authorised `ACTION_REQUEST`, perform the requested form/slot/upload/availability action before a courtesy reply. A reply is not required merely because a message arrived.

## 6. Form execution

`CAPTURE_ALL_STEPS → MAP_FIELDS → VALUE_PACK → PREFILL → QA → SUBMIT → RECEIPT`.

Unknown sensitive/legal/medical/affiliation facts cause STOP. Authentication != PREFILL permission; PREFILL != Submit permission; WriterAuthorization != external capability.

## 7. Evidence truth

Keep distinct:

`EMAIL_CANDIDATURE_SENT ≠ FORM_SUBMITTED ≠ SUBMITTED_CONFIRMED ≠ ACKNOWLEDGED ≠ SELECTED ≠ ACCEPTED_BY_USER ≠ TRAVEL_APPROVED ≠ ATTENDED ≠ CERTIFIED`.

Never promote state from a draft, open form, calendar event, semantic similarity or agent assertion.

## 8. Multi-agent safety

Every writer uses a fresh Session ID and bootstrap version from current main. Resolve the complete applicable global barrier state before WriterAuthorization. Missing/stale/conflicting/ACTIVE barrier authority fails closed. Recheck barrier state immediately before lease acquisition and irreversible promotion/provider effect.

A lease fences its exact scope only. Parallel research is allowed on disjoint scopes; external action WIP per application is 1.

## 9. RuntimeGraph / semantic boundary

RuntimeGraph and semantic systems are derived. Compare their watermark/source revision to current EventBus/provider evidence before use. Never mutate canonical CRM to make an old projection agree. Semantic retrieval may propose candidates; exact-ID evidence authorises mutations.

## 10. Human stop conditions

Stop only for a genuinely applicant-owned or external gate such as sensitive facts, legal consent, auth/PNR, final applicant-owned text, provider contradiction, payment, or irreversible Submit capability. Search private profile/evidence first; do not ask the user for facts already persisted.

## 11. Current durable pointers

- `plans/2026-09-21-portfolio-convergence-master.md` — current executable convergence graph.
- newest checkpoint under `checkpoints/` — continuity seal only; verify against live authority.
- `docs/APPLICATION_EXECUTION_CONTRACT.md` — detailed application law.
- `docs/SEMANTIC_BRAIN_DURABILITY.md` — semantic recovery law.
- `agent_context/bootstrap_manifest.json` — machine-readable writer bootstrap.

## 12. Fixed-point rule

Continue while any safe, current, non-colliding work unit exists. Do not stop merely because one opportunity is human-blocked: steal another disjoint ready work unit. End only after persisting exact blockers, receipts/readbacks, released leases, handoff and the next transition.
