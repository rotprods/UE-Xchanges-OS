# Read First — Active Execution

When `LIVE-STATE-OVERRIDE.json` exists with an active status, read it before using deadline/status fields from `goal-state.json`.

Private operational truth remains in the Drive CRM and opportunity/application dossiers. Todoist is a task projection, not the source of truth.

For the 28 August 2026 wave, the active override records:
- Triglav deadline crossed with receipt unresolved;
- Thrive original infopack verified;
- Step Into Paralympics target-profile conflict pending organiser reply;
- external messages sent and current active queue.
