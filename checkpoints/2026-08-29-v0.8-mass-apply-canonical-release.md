# Checkpoint — v0.8.0 canonical mass-apply release

Date: 2026-08-29T18:30:51+02:00  
State: `MASS_APPLY_FACTORY_CANONICAL`

## Purpose

Reconcile the live private CRM with GitHub, retire incompatible temporary overrides and produce a truthful portable release for the APPLY EVERYTHING VIABLE campaign.

## Verified live CRM state

- canonical opportunity IDs: **159**;
- application/dossier nodes: **148**;
- Mass_Apply_Queue rows: **148**;
- non-terminal queue rows: **140**;
- objective terminal rows: **8**;
- Source_Inbox nodes: **93**;
- organisation nodes: **17**;
- execution events: **24**;
- outcomes: **4**;
- submission receipts: **0**;
- verified TOY references: **0**.

## Provider distribution

- SALTO_ETC: **112**
- EYP_ESC: **14**
- ORGANISATION_WATCH: **8**
- EXISTING_CRM: **7**
- EURODESK: **6**
- NON_SALTO_ORG_CALL: **1**

## Release outputs

- authoritative live CRM exported as a v0.8 snapshot;
- one dossier skeleton per queue row: **148**;
- one machine-readable application state per queue row: **148**;
- submission queue, urgent queue, source coverage report and checksum manifest;
- Drive release folder under `09_AGENT_HANDOFF/RELEASES/`;
- canonical GitHub state, README, goal, AGENTS, Drive map and mass-apply policy.

## State repair

The previous `goal-state.json` projected the older 36/15 v0.7 campaign while the private CRM had already advanced. `LIVE-STATE-OVERRIDE.json` and `MASS_APPLY_OVERRIDE.md` also contained incompatible interim counts.

The live sheet contains 160 physical data rows under `Opportunities`, but one is blank; the verified canonical count is **159 nonblank Opportunity IDs**. This release corrects the dashboard projection rather than preserving the inflated count.

This release integrates the surviving rules and current aggregate facts into `goal-state.json`, then retires both root overrides.

## Quality truth

- CRM counts were read from the live native Sheet.
- The portable workbook is an export of the live CRM with a Release_Manifest sheet.
- Unsupported Google-Sheets dynamic-array formulas in the exported Decision_Queue were frozen to release values; the final portable workbook formula-error scan returned zero matches.
- Earlier local bundles with 36 dossiers / README-only drafts are marked superseded.
- Local repository tests were not run because the execution container could not resolve GitHub for cloning.
- Exact-head GitHub Actions remains the mandatory merge gate.
- No submission is claimed; receipts remain 0.

## Integrity

No current youth-work, trainer/facilitator, language, degree, organisation mandate, availability, fewer-opportunities or sensitive claim was inferred. No final AI prose was generated for `AI_UNKNOWN` calls. Dossier skeletons are labelled as preparation artifacts.

## Next transition

Execute every non-terminal T0/T1 row in deadline order, reduce form/infopack/AI-policy debt, complete human gates, submit legitimately and store receipts. Continue through T2–T4 until each row reaches receipt-backed submission or a sourced objective terminal state.
